name: Recent Activity

on:
  schedule:
    - cron: "0 * * * *" # Runs every hour
  workflow_dispatch: # Allow manual trigger

jobs:
  update-activity:
    runs-on: ubuntu-latest
    steps:
      - uses: Readme-Workflows/recent-activity
        with:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
